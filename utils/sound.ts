// Sound effects have been temporarily disabled due to playback issues.
// These functions are now no-ops to prevent errors.

export const playClickSound = () => {
  // No-op
};

export const playSubmitSound = () => {
  // No-op
};

export const playRevealSound = () => {
  // No-op
};
